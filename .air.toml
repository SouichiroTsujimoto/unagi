root = "."
tmp_dir = "tmp"

[build]
entrypoint = ["./tmp/main"]
cmd = "go tool templ generate -path internal/web && CGO_ENABLED=0 go build -o ./tmp/main ./cmd/server"
delay = 500
exclude_dir = ["tmp", "bin", "tools", ".git"]
# styles.css is compiled by css-watch → static/app.css; only app.css should trigger a Go rebuild.
# Keep watching Go under internal/web/css (themes.go); exclude the CSS source only.
exclude_regex = ["_templ\\.go", "styles\\.css$"]
# js: islands are go:embed'd; edits must rebuild tmp/main.
include_ext = ["go", "templ", "css", "js"]
# httpserver closes the listener on SIGINT immediately; keep a short grace
# window only for slow process-group teardown under Air's sh -c wrapper.
kill_delay = "1s"
send_interrupt = true
stop_on_error = true

[log]
# false: show colored build/runner/watcher logs (needed by the dev TUI air panel).
main_only = false
time = false

[misc]
clean_on_exit = true
startup_banner = ""
